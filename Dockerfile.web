# 前端 Web Dockerfile
# 構建階段
FROM node:18-alpine AS builder

WORKDIR /app

# 複製 package.json
COPY client/package*.json ./

# 安裝依賴
RUN npm ci

# 複製源代碼
COPY client/ ./

# 構建應用
RUN npm run build

# 生產階段
FROM nginx:alpine

# 複製構建結果
COPY --from=builder /app/build /usr/share/nginx/html

# 複製 nginx 配置
COPY nginx/default.conf /etc/nginx/conf.d/default.conf

# 複製 Service Worker
COPY client/public/sw.js /usr/share/nginx/html/sw.js

# 暴露端口
EXPOSE 80

# 啟動 nginx
CMD ["nginx", "-g", "daemon off;"]